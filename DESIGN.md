# دستیار اکسون — سند طراحی (Design Document)

> سیستم خودکار جمع‌آوری اطلاعات، تحلیل، و چیدن برنامه‌ی روزانه برای حسن فدوی جهرمی
> شرکت تجهیزات فیزیوتراپی Exon Medical — [exonmedical.com](https://exonmedical.com)

نسخه: ۰.۱ (پیش‌نویس) — تاریخ: ۱۴۰۵/۰۳/۰۹ (2026-05-30)

---

## ۱. هدف (Goal)

ساختن یک سیستم که **به‌صورت خودکار**:
1. داده‌ها را از منابع پراکنده‌ی حسن جمع کند (روبیکا، اس‌ام‌اس، دیدار، و در آینده ویس جلسات).
2. آن‌ها را با هوش مصنوعی (Claude) تحلیل کند.
3. دو خروجی روزانه تولید کند:
   - **① برنامه‌ی روزانه‌ی شخصی** (کار + خانواده + کارهای شخصی).
   - **② گزارش عملکرد نیروها** بر اساس گروه‌های «پروسیجر» روبیکا.
4. خروجی را به دست حسن برساند (کانال «برنامه‌ی من» در روبیکا).

**درد اصلی که حل می‌کنیم:** اطلاعات زیاد و پراکنده است؛ جلسات طولانی می‌شوند و دیتا گم می‌شود؛ معلوم نیست هر نیرو واقعاً کار کرده یا وقت تلف کرده؛ و حسن وقت کافی برای جمع‌بندی همه‌ی این‌ها و برنامه‌ریزی ندارد.

---

## ۲. پروفایل کاربر (حسن)

| موضوع | جزئیات |
|-------|--------|
| نقش | هم‌بنیان‌گذار Exon Medical (با برادرش مهدی، از ۱۳۹۴) |
| تمرکز فعلی | فروش + حسابداری؛ عملاً همه‌کاره (بازرگانی، واردات، نمایشگاه، بازاریابی، تأمین، R&D با هوش مصنوعی) |
| اندازه‌ی شرکت | ~۴۰ نفر |
| تیم نزدیک | مهدی (برادر)، آقای قربانی (کمک در همه‌ی بخش‌ها) |
| ساختار | ۳ فروش، ۳ حسابداری، چند نفر گزارش تولید (در دیدار)، ۱ نفر بازرگانی/استاندارد |
| پروژه‌های ویژه | آپدیت UI دستگاه‌ها با AI، پروژه‌ی پهپاد کشاورزی دولتی (FPV، مسیریابی آفلاین، ترکینگ) |

### برنامه‌ی روزانه‌ی فعلی
- **~۲:۴۵** (هدف، ولی معمولاً خواب می‌ماند): نماز شب، مناجات، نماز صبح
- **~۷:۰۰**: بیداری
- **۸–۹**: رسیدن به شرکت
- **تا ~۱۴**: کارهای روتین (فروش/حسابداری/متفرقه)
- **بعد از ۱۴**: R&D (با کمک ایجنت Claude)
- **۱۶:۳۰**: پایان رسمی کار نیروها
- **برگشت خانه**: متغیر — گاهی ۱۷–۱۸، گاهی ۲۰، گاهی تا نیمه‌شب

### خانواده
- **پسر (سام، ۴ ساله):** صبح‌ها مادرخانم پیشش است → ساعت ۱۲ مهد → ساعت ۵ خانم می‌آوردش.
- **همسر:** ۸ صبح سرِ کار.
- **مادر:** یک کوچه پایین‌تر زندگی می‌کند؛ ۲ ماه پیش پایش شکسته، تازه با واکر راه می‌رود (نیاز به رسیدگی).
- **پدر (خدا رحمتش کند):** پنجشنبه/جمعه بهشت‌زهرا — اول مزار شهدا، بعد پدر (یا برعکس).

### اهداف شخصی
- بیدار شدن منظم برای نماز شب (چالش اصلی).
- تعادل بین کارِ شرکت، رسیدگی به مادر، و وقت برای سام.

---

## ۳. دارایی‌های موجود (Existing Assets)

سه مخزن گیت‌هاب موجود است:

### `jdjv` — اپ اندرویدی SmsBot
اپ اندرویدی که روی یک گوشیِ ۲۴ساعته اجرا می‌شود. شامل:
- **`RubikaClient.java`** — کلاینت کامل API روبیکا (v6): `getChats`، `getMessages`، `sendMessage`، `forwardMessages`، `getObjectTitle`، احراز هویت (sendCode/signIn با RSA+AES). ✅
- **`SmsReceiver` / `SmsLogger` / `SmsBotService`** — دریافت/خواندن/پاسخ خودکار اس‌ام‌اس. ✅
- **`ClaudeApiClient.java`** — اتصال به Claude API (هم برای اس‌ام‌اس، هم برای پاسخ در گروه روبیکا). ✅
- **`BotRules.java`** — پاسخ‌های کلیدواژه‌ای ساده (fallback).
- **`JobVisionActivity` / `Applicant`** — اتوماسیون سایت استخدام jobvision.ir (خواندن رزومه‌ها، ارسال SMS به متقاضیان).
- **`BootReceiver`** — اجرای خودکار بعد از روشن شدن گوشی.
- `joystick_reader.py` — مربوط به پروژه‌ی پهپاد (مستقل).

### `didar` — اپ DidarQC + اسکریپت پایتون
کلاینت API دیدار (CRM):
- **`DidarApiClient.java`** / `didar_qc_report.py`
- Endpointها:
  - `POST /activity/search?apikey=...` → فعالیت‌ها (فیلدها: `Title`, `Description`, `ActivityTypeName`, `ActivityDate`, `ContactName`, `DealTitle`, `OwnerName`, `StatusName`)
  - `POST /deal/search?apikey=...` → معاملات (`PipelineStageName`, `Amount`, `StatusName`, ...)
  - `POST /activitytype/list?apikey=...` → انواع فعالیت
  - الگوی صفحه‌بندی: `Criteria: { From, Limit, Keywords | ActivityTypeId }`، پاسخ در `ResponseData` یا `Data`.
- Base URL: `https://app.didar.me/api`

### `claude-daily-task` — مخزن هدف (همین‌جا)
خالی است؛ خانه‌ی نهایی سیستم یکپارچه می‌شود.

---

## ۴. ⚠️ مسائل امنیتی (باید قبل از هر چیز حل شوند)

هر سه مخزن **پابلیک** هستند و این رازها لو رفته‌اند:

| # | راز | محل | اقدام لازم |
|---|-----|-----|------------|
| ۱ | کلید Anthropic API (`sk-ant-api03-…`) | `jdjv/ClaudeApiClient.java` | **باطل (revoke) و کلید نو** |
| ۲ | ایمیل + رمز JobVision (`@Mahdi1365`) | `jdjv/JobVisionActivity.java` | **تغییر رمز** |
| ۳ | کلید API دیدار (`231020a4-…`) | `didar/DidarApiClient.java` + پایتون | **کلید نو در پنل دیدار** |

**قواعد امنیتی این پروژه:**
- هیچ رازی داخل سورس‌کد قرار نمی‌گیرد.
- رازها و تنظیمات حساس در **صفحه‌ی تنظیمات داخل اپ** (ذخیره در `SharedPreferences`) یا `local.properties` (که در `.gitignore` است) نگه‌داری می‌شوند.
- پیشنهاد: هر سه مخزن قدیمی **پرایوت** شوند.

---

## ۵. معماری (Architecture)

**تصمیم کلیدی:** به‌جای ساختن سرور ابری جدا، روی **همان گوشی ۲۴ساعته** که SmsBot رویش است یک اپ یکپارچه می‌سازیم. دلیل: آن گوشی همین حالا به روبیکا (با حساب حسن) و اس‌ام‌اس وصل است و Claude دارد — بهترین میزبان است.

```
┌──────────────── دستیار اکسون (اپ اندروید، گوشی ۲۴ساعته) ────────────────┐
│                                                                          │
│  منابع ورودی                موتور                     خروجی              │
│  ───────────                ─────                     ──────             │
│  RubikaClient (گروه‌ها) ┐                                                 │
│  📥 کانال اینباکس ──────┤                                                 │
│  SmsReader ─────────────┼─► Collector ─► Claude ─► ① برنامه‌ی روزانه ─┐   │
│  DidarApiClient ────────┤   (جمع‌آوری     (تحلیل)   ② گزارش نیروها     │   │
│  (فاز۲) VoiceSTT ───────┘    روزانه)                                  ▼   │
│                                                   Deliverer ─► 🗓️ کانال برنامه │
│                                                                          │
│  Scheduler (AlarmManager): صبح ۷ + عصر  ──► کل این جریان را اجرا می‌کند    │
│  SettingsActivity: رازها، پروفایل، GUID گروه‌ها، زمان‌بندی، کانال خروجی    │
└──────────────────────────────────────────────────────────────────────────┘
```

### ماژول‌ها

| ماژول | وظیفه | منبع |
|-------|-------|------|
| `RubikaClient` | خواندن پیام گروه‌ها و اینباکس، ارسال به کانال برنامه | موجود (jdjv) |
| `SmsReader` | خواندن اس‌ام‌اس‌های امروز | منطقش در jdjv هست |
| `DidarApiClient` | فعالیت‌ها و معاملات روز | موجود (didar) |
| `DailyCollector` | جمع‌آوری همه‌ی منابع در یک ساختار واحد | **جدید** |
| `DailyOrganizer` | ساخت prompt و فراخوانی Claude برای دو خروجی | **جدید** |
| `Deliverer` | فرستادن خروجی به کانال «برنامه‌ی من» | بر پایه‌ی `sendMessage` موجود |
| `OrganizerScheduler` | اجرای خودکار صبح و عصر (`AlarmManager`) | **جدید** |
| `SettingsActivity` | پیکربندی رازها/پروفایل/گروه‌ها/زمان | **جدید** |

---

## ۶. منابع داده و نحوه‌ی جمع‌آوری

### ۶.۱ روبیکا — گروه‌های پروسیجر و مدیریت
- هر نیرو یک گروه «پروسیجر» دارد؛ هر ۱۵ دقیقه آپدیت کار می‌گذارد (متن + ویس + عکس + برنامه‌ی صبح/پایان).
- یک گروه «مدیریت داخلی» (حسن، مهدی، آقای قربانی).
- گروه «فروش».
- **روش:** برای هر گروه `getMessages(guid, minId)` صدا زده می‌شود؛ `minId` آخرین پیام پردازش‌شده ذخیره می‌شود تا فقط پیام‌های جدید خوانده شوند.
- پیام‌های متنی مستقیم استفاده می‌شوند. عکس‌ها فعلاً فقط شمارش/اشاره می‌شوند. ویس‌ها در فاز ۲.

### ۶.۲ اس‌ام‌اس
- خواندن از `content://sms/` (منطقش در `ClaudeApiClient.buildMessages` هست).
- فیلتر روی اس‌ام‌اس‌های امروز؛ دسته‌بندی بانکی/مشتری/یادآوری توسط Claude.

### ۶.۳ دیدار
- `activity/search` و `deal/search` برای بازه‌ی امروز (یا کلیدواژه‌های مهم مثل وضعیت فروش، «برگشت از QC»).
- نگاشت به خلاصه‌ی وضعیت فروش/تولید.

### ۶.۴ (فاز ۲) ویس جلسات
- ویس‌ها از گروه‌های روبیکا یا فایل دستی.
- نیاز به سرویس **تبدیل گفتار به متن (STT) فارسی** (مثلاً Whisper). این یک وابستگی/هزینه‌ی جدید است و در فاز ۲ تصمیم‌گیری می‌شود.

---

## ۶٫۵ لایه‌ی ثبت ورودی — «اینباکس» شخصی حسن (Capture Layer)

همه‌ی منابع بالا «بیرونی» هستند و خودکار خوانده می‌شوند. اما هر چیزی که **در ذهن حسن است یا برای خودش اتفاق می‌افتد** (کار لحظه‌ای، تعامل با آدم‌ها، ضبط صدا، مشکل خانه، جزئیات روز) هیچ مسیری به سیستم ندارد. این لایه آن حفره را پر می‌کند.

### تصمیم: دو کانال خصوصی روبیکا

از همان حساب روبیکای حسن (که اپ بهش وصل است) دو کانال/گروه خصوصی ساخته می‌شود:

| کانال | کاربرد | جهت |
|-------|--------|-----|
| 📥 **اینباکس** | حسن هر چیزی را اینجا می‌ریزد: متن، **ویس**، عکس، فوروارد | ورودی (اپ می‌خواند) |
| 🗓️ **برنامه‌ی من** | دستیار اینجا برنامه‌ی روزانه + گزارش نیروها + تأیید دسته‌بندی‌ها را می‌گذارد | خروجی (اپ می‌نویسد) |

- **صفر ابزار جدید، صفر یادگیری** — حسن همان روبیکای همیشگی را دارد.
- اپ کانال اینباکس را با `getMessages` می‌خواند (مثل گروه‌های پروسیجر) و در کانال «برنامه‌ی من» با `sendMessage` می‌نویسد.
- ویس‌ها همین‌جا (فاز ۲) به متن تبدیل می‌شوند؛ عکس‌ها فعلاً ضمیمه/اشاره می‌شوند.

### دسته‌بندی: بدون قانون، خودکار

حسن **طبیعی** می‌نویسد یا ویس می‌دهد؛ هیچ فرمت/هشتگ اجباری نیست. Claude خودش هر آیتم را به یکی از دسته‌ها نگاشت می‌کند:

```
کاری: فروش · حسابداری · تولید · R&D · بازرگانی/واردات · نمایشگاه · متفرقه‌ی شرکت(تاسیسات…)
خانواده: سام · مادر · همسر · پدر/بهشت‌زهرا
شخصی: سلامت · معنوی(نماز شب) · خانه/تعمیرات
```

و نوع هر آیتم را هم تشخیص می‌دهد: **تسک** | **رویداد** | **تعامل** | **یادداشت** — همراه با اولویت و ددلاین (اگر معلوم باشد).

### نمونه‌ی جریان ورودی‌ها

| حسن در اینباکس می‌گذارد | دستیار چه می‌کند |
|--------------------------|-------------------|
| «کولر گازی نصب امروز هماهنگ شد» | دسته: 🏢 متفرقه‌ی شرکت/تاسیسات → تسک امروز |
| «با آقای X درباره‌ی Y حرف زدم، قرار شد فلان» | ثبت در `interactions` (با کی/چی/کِی)؛ اگر مشتری → پیشنهاد ثبت در دیدار |
| فایل صوتیِ ضبطِ مکالمه | (فاز ۲) متن + خلاصه + استخراج کارها |
| «فلان چیز تو خونه اذیتم می‌کنه» | دسته: 🏠 خانه → ساخت پلن گام‌به‌گام |

### حافظه و جلوگیری از تکرار (Dedup)

دستیار یک حافظه‌ی ساختارمند روی همان گوشی نگه می‌دارد:
`tasks[] · events[] · interactions[] · notes[]` (به‌صورت فایل JSON یا دیتابیس سبک).

- هر ورودی جدید قبل از ساخت، با موجودی‌ها **مقایسه (شباهت متنی)** می‌شود؛ اگر تکراری بود **آپدیت** می‌شود نه دوباره‌سازی.
- برای مشتری‌ها منبعِ حقیقت **دیدار** است تا داده دوتایی نشود.
- `interactions` بر اساس «شخص + موضوع» کلیدگذاری می‌شود تا تاریخچه‌ی هر رابطه یکجا بماند.

---

## ۷. جریان روزانه (Daily Flow)

دو بار در روز اجرا می‌شود:

**صبح (~۷:۰۰):**
1. `DailyCollector`: پیام‌های جدید گروه‌ها از دیشب تا حالا + اس‌ام‌اس‌های شب + فعالیت‌های باز دیدار را جمع می‌کند.
2. `DailyOrganizer`: همه را + پروفایل حسن + تاریخ/روز هفته به Claude می‌دهد.
3. خروجی **برنامه‌ی روزانه**: بلوک‌های زمانی پیشنهادی (نماز، شرکت، روتین تا ۱۴، R&D، رسیدگی به مادر، وقت سام، و اگر پنجشنبه/جمعه است یادآوری بهشت‌زهرا) + لیست اولویت‌دار کارها.
4. `Deliverer`: به کانال «برنامه‌ی من» می‌فرستد.

**عصر (مثلاً ۱۷:۰۰):**
1. پیام‌های کل روزِ گروه‌های پروسیجر جمع می‌شود.
2. خروجی **گزارش عملکرد نیروها**: برای هر نیرو — آیا برنامه‌ی صبح/پایان گذاشت؟ چند آپدیت داشت؟ کارهای انجام‌شده، شکاف‌ها/وقت تلف‌شده، و پرچم‌قرمزها.
3. به کانال «برنامه‌ی من» می‌فرستد.

---

## ۸. نمونه‌ی فرمت خروجی

### ① برنامه‌ی روزانه (کانال برنامه)
```
🗓️ برنامه‌ی پنجشنبه ۹ خرداد

⏰ اولویت‌های امروز
  1. [فروش] پیگیری چک مشتری‌های معوق (از گروه حسابداری)
  2. [R&D]  ادامه‌ی UI دستگاه X
  3. [خانواده] سر زدن به مادر (دارو/واکر)

🕒 بلوک‌های زمانی
  08:30–14:00  شرکت — روتین فروش/حسابداری
  14:00–16:30  R&D
  پنجشنبه →    بهشت‌زهرا (شهدا + پدر)
  عصر →        وقت سام

📩 از منابع امروز
  • ۳ اس‌ام‌اس بانکی، ۱ پیام مشتری مهم
  • دیدار: ۲ معامله در مرحله‌ی نهایی
```

### ② گزارش عملکرد نیروها (کانال برنامه)
```
👥 گزارش نیروها — ۹ خرداد

✅ نجیبه تاجیک   | برنامه صبح ✔  پایان ✔  | ۱۲ آپدیت | کارها: ارسال شبا، استابلایزر آناهیتا
⚠️ نیروی ب      | برنامه صبح ✖           | ۳ آپدیت  | شکاف ۲ ساعته بدون گزارش
🔴 نیروی ج      | بدون آپدیت از ۱۱ صبح
```

---

## ۹. پیکربندی (Settings)

همه در `SharedPreferences` (یا `local.properties` گیت‌ایگنور شده):
- کلید Anthropic API
- کلید API دیدار
- لیست GUID گروه‌های پروسیجر + گروه مدیریت + فروش (با برچسب نام نیرو)
- GUID کانال **اینباکس** (ورودی) و GUID کانال **برنامه‌ی من** (خروجی)
- زمان اجرای صبح و عصر
- متن پروفایل شخصی (خانواده/مادر/نماز/سام/بهشت‌زهرا) که در prompt تزریق می‌شود

---

## ۱۰. فازبندی (Roadmap)

**فاز ۱ — پایه (در دست انجام):**
- [ ] آوردن کد `jdjv` به `claude-daily-task` با حذف رازها + `SettingsActivity` و `.gitignore`
- [ ] افزودن `DidarApiClient` (از مخزن didar)
- [ ] `DailyCollector` (روبیکا + SMS + دیدار)
- [ ] `DailyOrganizer` + promptهای دو خروجی
- [ ] `Deliverer` (کانال «برنامه‌ی من»)
- [ ] `OrganizerScheduler` (AlarmManager صبح/عصر)
- [ ] تزریق پروفایل حسن

**فاز ۲ — توسعه:**
- [ ] تبدیل ویس جلسات به متن (STT فارسی)
- [ ] تحلیل عکس‌های گزارش کار
- [ ] داشبورد/صفحه‌ی خلاصه در اپ
- [ ] یادآور هوشمند نماز شب

---

## ۱۱. پرسش‌های باز (Open Questions)

1. ~~کانال خروجی نهایی~~ ✅ **حل شد:** دو کانال خصوصی روبیکا — 📥 اینباکس (ورودی) و 🗓️ برنامه‌ی من (خروجی). دسته‌بندی خودکار بدون قانون.
2. **GUID گروه‌ها و کانال‌ها:** لازم است GUID گروه‌های پروسیجر/مدیریت/فروش + دو کانال اینباکس و برنامه مشخص شود (می‌توان از `getChats` خودکار لیست کرد و حسن انتخاب کند).
3. **STT فاز ۲:** کدام سرویس؟ هزینه؟ (Whisper API / سرویس ایرانی)
4. **حریم خصوصی نیروها:** رصد عملکرد باید شفاف/منصفانه باشد؛ معیارها با حسن نهایی شود.
5. **مدل Claude و بودجه:** کدام مدل و سقف هزینه‌ی روزانه؟
```
